# Noetix Motion Editor Extensions

Noetix Motion Editor 的 Blender 扩展发布库。

此仓库仅用于提供 Windows x64 扩展安装包、版本更新索引和发布说明，不包含插件源码。

## 安装与更新

在 Blender 中打开：

`编辑 → 偏好设置 → 获取扩展 → 存储库`

添加远程存储库后，使用本仓库根目录的 `index.json` 地址即可安装和接收更新。

扩展包由私有源码仓库自动构建并发布，请勿直接修改本仓库中的扩展包或 `index.json`。
